version: '3'

services:
  backend:
    build:
      context: ./backend
      dockerfile: DockerFile
    ports:
      - "3000:3000"

  frontend:
    build:
      context: ./frontend
      dockerfile: DockerFile
    ports:
      - "80:80"
